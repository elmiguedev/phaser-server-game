{
  "scripts": {
    "game-dev": "parcel watch src/_game/index.html --dist-dir src/public",
    "server-dev": "nodemon src/app.js",
    "dev": "concurrently \"npm run game-dev\" \"npm run server-dev\"",
    "build": "parcel build src/_game/index.html --dist-dir src/public"
  },
  "devDependencies": {
    "nodemon": "^2.0.12",
    "parcel": "^2.0.0-beta.3.1",
    "concurrently": "^6.2.0"
  },
  "dependencies": {
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "phaser": "^3.55.2",
    "socket.io": "^4.1.3"
  }
}
